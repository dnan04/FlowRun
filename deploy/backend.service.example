[Unit]
Description=Data Runner Platform Backend
After=network.target

[Service]
WorkingDirectory=/opt/data-runner/backend
ExecStart=/opt/conda/envs/py311_ai/bin/python -m uvicorn app.main:app --host 0.0.0.0 --port 8000
Restart=always
User=www-data

[Install]
WantedBy=multi-user.target
